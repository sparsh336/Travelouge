<% layout('layouts/boilerplate')  %> 

<div id="map"  style="width: 100%; height:500px "></div>

    <h1>All campgrounds</h1>

<ul>
    <% for(let camp of c){ %> 
    <div class="card mb-3">
        <div class="row">
            <div class="col-md-4">
               <img class="img-fluid" src="<%= camp.image %> " alt="">
             </div>
             <div class="col-md-8">
              <div class="card-body">
                  <h3 class="card-title"><%=camp.title  %> </h3>
                  <p class="card-text"> <%=camp.description  %> </p>
                  <p class="card-text text-muted"><%= camp.location %>  </p>
              </div>
              <div class="cars-footer pl-3">
                  <a class="btn btn-primary" href="/campgrounds/<%= camp._id %> ">visit</a>
              </div>

             </div>
        </div>
    </div>
    <%}%> 
</ul>
<script>
      const mapToken = '<%-process.env.MAPBOX_TOKEN%>';
      const campground= {features:<%-JSON.stringify(c)%>}
    
</script>
<script src="/js/cluster.js"></script>



